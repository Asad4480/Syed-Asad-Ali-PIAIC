9/*Stripping Names: Store a person’s name, and include some whitespace characters at the beginning and end of the name. 
Make sure you use each character combination, "\t" and "\n", at least once. 
Print the name once, so the whitespace around the name is displayed. 
Then print the name after striping the white spaces.*/
let firstLetter: string = "A";
let secondLetter: string = "S";
let thirdLetter : string = "A";
let fourthLetter : string = "D";
let myName = `${firstLetter}\n\t${secondLetter}\n\t\t${thirdLetter}\n\t\t\t${fourthLetter}`;
console.log(myName);


